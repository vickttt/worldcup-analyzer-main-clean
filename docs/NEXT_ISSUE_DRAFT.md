# Next Issue Draft

Date: 2026-06-22

Generated by: WorldCup Supervisor

## Recommended Issue Title

```text
[Automation] Harden agent workflow status and commit hygiene
```

## Recommended Goal

Prepare the next low-risk GitHub Issue after the first successful Issue -> Branch -> PR rehearsal.

The goal is to harden the automation workflow by committing the current automation status report and documenting the remaining workflow guardrails, without modifying product code or betting logic.

## Recommended Scope

- Add or update `AUTOMATION_STATUS_REPORT.md`.
- Review current dirty worktree state.
- Confirm `data/performance_logs/app_performance.jsonl` is not included.
- Confirm no business code, ranking logic, recommendation logic, scripts, modules, or historical data files are modified.
- Use a feature branch and Pull Request.
- Let GitHub Actions Agent QA run.
- Leave merge decision to Jin.

## Allowed Changes

- `AUTOMATION_STATUS_REPORT.md`
- `docs/NEXT_ISSUE_DRAFT.md` only if the Issue explicitly includes Supervisor draft synchronization
- `docs/CHANGELOG.md` and `docs/QA_REPORT.md` only if Jin wants governance docs updated for this documentation task

## Forbidden Changes

- `app.py`
- `scripts/`
- `modules/`
- `data/history/`
- `data/history/backfill/`
- `data/performance_logs/app_performance.jsonl`
- `.env`
- secrets, tokens, keys
- `strategy_score(...)`
- `strategy_comparison(...)`
- `evaluate_allocation(...)`
- production sorting
- recommendation logic
- default recommendation logic
- API refreshes
- branch deletion
- force push
- merge by agent

## Acceptance Criteria

- PR changes only the allowed documentation file(s).
- `data/performance_logs/app_performance.jsonl` is not staged or committed.
- PR clearly states that no business code changed.
- PR clearly states that no data files changed.
- PR links and closes the GitHub Issue.
- GitHub Actions Agent QA passes.
- Jin can review and merge manually.

## QA Requirements

- Run `git status --short` before staging.
- Run `git diff --name-only` before staging.
- Confirm staged files with `git diff --cached --name-only`.
- Confirm no changed files under:
  - `app.py`
  - `scripts/`
  - `modules/`
  - `data/history/`
  - `data/performance_logs/app_performance.jsonl`
- No Python syntax check is required unless Python files are changed.
- No API calls should be run.

## Risk Level

Low

Reason: documentation-only automation hardening task. No product behavior, ranking behavior, recommendation behavior, UI behavior, or data behavior should change.

## Suggested Commit Message

```text
Add automation status report
```

## Supervisor Notes

The first real workflow rehearsal has already succeeded:

- Issue #1 created.
- Branch `codex/1-agent-workflow-smoke-test` created.
- PR #2 opened.
- Agent QA passed.
- PR #2 was merged.

The next useful step is to preserve the resulting automation status and then harden the remaining workflow gaps:

- branch protection confirmation
- local pre-commit guardrails
- local changed-file checker
- Supervisor-generated PR risk review

